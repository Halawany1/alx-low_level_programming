#include "main.h"
/**
 * add -  a function that adds two integers and returns the result.
 * @a : number one
 * @b : number two
 * Return: times table
 * add extra space past single digit
 */
int add(int a, int b)
{
return (a + b);
}
